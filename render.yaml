services:
  - type: web
    name: studenthub-api
    runtime: java
    plan: free
    buildCommand: cd backend && ./mvnw package -DskipTests
    startCommand: java -jar backend/target/student-management-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: "17"
